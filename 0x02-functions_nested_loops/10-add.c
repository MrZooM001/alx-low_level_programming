#include "main.h"

/**
 * add - prints every minute of the day in 24h format
 * @a: An integer number to be added as first operand
 *@b: An integer number to be added as second operand
 *
 * Return: the result of sum a + b
 */
int add(int a, int b)
{
int sum;
sum = a + b;
return (sum);
}
